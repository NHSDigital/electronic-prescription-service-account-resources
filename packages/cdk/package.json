{
  "name": "cdk",
  "version": "0.1.0",
  "scripts": {
    "cdk-synth": "cdk synth  --output ../../cdk.out --quiet --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\"",
    "cdk-diff": "cdk diff ${CDK_STACK_NAME}  --output ../../cdk.out --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\"",
    "cdk-deploy": "cdk deploy ${CDK_STACK_NAME} --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\" --ci true --require-approval ${REQUIRE_APPROVAL}",
    "cdk-prepare-changeset": "cdk deploy ${CDK_STACK_NAME} --method prepare-change-set --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\" --ci true --require-approval ${REQUIRE_APPROVAL}",
    "cdk-flags": "cdk flags --unstable=flags --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\"",
    "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs .",
    "tsx": "tsx"
  },
  "dependencies": {
    "@nhsdigital/eps-cdk-constructs": "^1.3.1",
    "aws-cdk-lib": "^2.238.0",
    "cdk-nag": "^2.37.55",
    "constructs": "^10.4.5"
  },
  "devDependencies": {
    "aws-cdk": "^2.1106.0",
    "tsx": "^4.21.0"
  }
}
